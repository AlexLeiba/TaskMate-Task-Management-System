name: Hello bro

on:
  push:
    branches:
      - staging
      - workflow
      - closed
  pull_request:
    types:
      - opened
      - synchronize
      - reopened

    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build step
        run: echo "Building..., ${{vars.MY_FIRST_SECRET_VAR}} ${{vars.ENVIRONMENT_VARIABLE_BRO}}"
      - name: Test step
        run: echo "Tusaneala...<<<<<<"

  test:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Test it
        run: echo "Test job bro"
