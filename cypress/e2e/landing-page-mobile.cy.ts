describe("Landing page mobile view", () => {
  beforeEach(() => {
    cy.viewport(375, 800);
    cy.visit("/");
  });

  //   test header
  // test overview click on each card
  // the rest is tested on desktop
});
