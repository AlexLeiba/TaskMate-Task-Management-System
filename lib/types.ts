export type ActivityType = {
  id: number;
  imageUrl: string;
  name: string;
  email: string;
  createdAt: number;
  activity: string;
  boardName: string;
  activityType: string;
};

export type BoardType = {
  id: string;
  title: string;
  imageUrl: string;
};
